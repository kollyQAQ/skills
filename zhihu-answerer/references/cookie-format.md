# Cookie 格式

推荐直接提供浏览器请求头中的 `Cookie` 原文，例如：

```text
z_c0=...; d_c0=...; _zap=...; q_c1=...; SESSIONID=...
```

## 最小建议

通常至少应包含登录态相关字段（示例）：
- `z_c0`
- `d_c0`
- `SESSIONID`（若存在）

实际字段以你当前浏览器登录态为准。

## 安全建议

- 仅在当前会话临时使用，不写入仓库。
- 过期后立即更新，不复用旧值。
- 只授予需要的最小权限账号。
